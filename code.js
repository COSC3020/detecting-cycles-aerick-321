function hasCycle(graph) {
    let minSpTree = new Set();
    let edge;
    for (let node = 0; node - 1> edge; node++){
        if(!minSpTree.has(cycle)){
            minSpTree.add(edge)
            return true;
        }
    }
    return false;
}

function detectCycle(){
    
}
